{
  "name": "shoppable-video-analytics",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack Shoppable Video Analytics Dashboard",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client && npm install",
    "seed": "npm run seed --prefix server",
    "dev": "concurrently -n \"SERVER,CLIENT\" -c \"cyan,magenta\" \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
